//https://www.acmicpc.net/problem/1541
#include <bits/stdc++.h>
using namespace std;

int Num_arr[55];
string Mass_arr[55];
string str;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> str;

    for (int i = 0; i < str.size(); i++) {
        if (str[i] == '+' || str[i] == '-' || str[i] == '\n')
            if ()
    }

}