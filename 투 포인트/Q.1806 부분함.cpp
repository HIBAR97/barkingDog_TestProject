//https://www.acmicpc.net/problem/1806
#include <bits/stdc++.h>
using namespace std;

int N, S;
int arr[100005];
int Result, End;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> N >> S;

    for (int i = 0; i < N; i++)
        cin >> arr[i];

    Result = arr[0];

    for (int Start = 0; Start < N; ++Start) {
        End++;
        if (End != N)

    }
}